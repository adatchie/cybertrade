padding: '5px 10px',
    fontSize: '0.8rem',
        cursor: 'pointer'
                                                }}
                                            >
    Sell
                                            </button >
                                        </>
                                    )}
<Trash2 size={20} color="#ff4444" onClick={() => handleDelete(item)} style={{ cursor: 'pointer' }} />
                                </div >
                            </div >
                        )}
                    </div >
                ))
            )}
        </div >
    );
};
